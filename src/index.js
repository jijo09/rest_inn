import React from 'react';
import ReactDOM from 'react-dom'; // must have import
import App from './components/App'; // we are importing this content to use as <App />

ReactDOM.render(<App />,document.getElementById('root'));
